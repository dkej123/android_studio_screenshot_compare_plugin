# First-run defaults for the Figma build variant only. This file exists solely in the `figma`
# source set, so the public build never sees it and starts with empty settings. Comma- or
# newline-separated; paths are relative to the project base directory.
goldenPaths=screenshotTests/src/test/screenshots
generatedPaths=screenshotTests/build/outputs/roborazzi
